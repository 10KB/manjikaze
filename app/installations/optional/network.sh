# Netcat to debug TCP connections
install_package "gnu-netcat" repo

# Parallel to run parallel network requests
install_package "parallel" repo

# Bind to resolve DNS
install_package "bind" repo

# Nmap to scan ports
install_package "nmap" repo
